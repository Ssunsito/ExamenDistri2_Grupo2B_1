<!-- *****************************************************
Proyecto Distri2 - Reserva.co
Vista _Layout.cshtml
Kenneth Pantoja
Santiago Pila
Fecha: 03/08/2025

// RESULTADOS FINALES
- Se implementó estructura base de la aplicación con Bootstrap
- Se integró sistema de gestión de sesiones en el encabezado
- Se estableció manejo de estilos y scripts personalizados
- Se implementó diseño responsivo con clases condicionales
- Se centralizó la gestión de recursos estáticos

// CONCLUSIONES
1. El layout proporciona una base sólida para todas las vistas
2. La gestión de sesiones mejora la experiencia del usuario
3. El sistema de estilos y scripts optimiza el rendimiento
4. El diseño responsivo asegura la compatibilidad multiplataforma
5. La estructura modular facilita el mantenimiento del código
    ***************************************************** -->


<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title</title>

    <!-- CSS globales -->
    <link rel="stylesheet" href="~/Content/bootstrap.min.css" />
    <link rel="stylesheet" href="~/Content/site.css" />
    <link rel="stylesheet" href="~/Content/Layout.css" />


    @RenderSection("styles", required: false)
</head>
<body class="@(ViewBag.BodyClass ?? "")">
    <header class="bg-dark text-white p-2 d-flex justify-content-between align-items-center">
        <div>
            <h4 class="m-0">Sistema de Reservas FIEE</h4>
        </div>
        <div>
            @if (Session["UserName"] != null)
            {
                <span class="me-3">👤 @Session["UserName"] (@Session["UserRole"])</span>
                <a href="@Url.Action("Logout", "Account")" class="btn btn-sm btn-outline-light">
                    Cerrar Sesión
                </a>
            }
        </div>
    </header>

    <main class="@(ViewBag.BodyClass == "login-page" ? "" : "main-content")">
        @RenderBody()
    </main>



    <footer class="bg-dark text-white text-center py-2 mt-5">
        © @DateTime.Now.Year - ProyectoDistri2
    </footer>

    <!-- Scripts globales -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    @RenderSection("scripts", required: false)
    </>
</html>
